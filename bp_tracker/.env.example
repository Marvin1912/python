# Flask app -> Postgres connection
BP_DB_HOST=localhost
BP_DB_PORT=5432
BP_DB_NAME=bptracker
BP_DB_USER=bp
BP_DB_PASSWORD=bp_dev_password
BP_DB_SCHEMA=bp

# Postgres container init (consumed by the db service in docker-compose only)
POSTGRES_DB=bptracker
POSTGRES_USER=bp
POSTGRES_PASSWORD=bp_dev_password

FLASK_PORT=5001
